<?
	var modelVariable = modelName.toLowerCase();
	var basePath = modelName.toLowerCase();
	var basePathPlural = controllerName.toLowerCase();
  var addIncl = namespace ? "../" : "" ;
  var nsPath = namespace ? namespace + "_" : "" ;
?>
<% layout('../default_layout') -%>
<!-- new page -->
<!-- page breadcrumb -->
<ul class="breadcrumb">
    <li><a href="/<?= nsPath ?>">Home</a> <span class="divider">/</span></li>
    <li><a href="<%- pathTo.<?= nsPath ?><?= basePathPlural ?>() %>"><?= controllerName ?></a> <span class="divider">/</span></li>
    <li class="active"><a href="#"><%- title %></a></li>
</ul>
<%- partial('<?= addIncl ?>../messages') %>
<!-- page title -->
<div class="page-header">
    <h1><%- title %></h1>
</div>

<!-- form -->
<form method="post" action="<%- pathTo.create_<?= nsPath ?><?= basePathPlural ?>() %>" class="form-horizontal">
    <input type="hidden" name="_method" value="post"/>
    <input type="hidden" name="_csrf" value="<%= session._csrf %>" />
    <%- partial('form',{ locals : { <?= modelVariable ?> : <?= modelVariable ?> } }) %>
    <div class="form-actions">
        <button class="btn btn-primary" type="submit" value="Update">
            <i class="icon-ok icon-white"></i>
            Save
        </button>
        <a class="btn button-cancel" href="<%- pathTo.<?= nsPath ?><?= basePathPlural ?>() %>">
            <i class="icon-tasks"></i>
            Cancel
        </a>
    </div>
</form>
<!-- end new page -->
